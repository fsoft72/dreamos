# Used by the INSTALLED system (Calamares target). The live session does
# not run LightDM - it uses tty1 autologin + startx. Here we only make
# the greeter preselect the Openbox session; login is a normal prompt.
[Seat:*]
user-session=openbox
greeter-session=lightdm-gtk-greeter
