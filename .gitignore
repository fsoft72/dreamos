# live-build working directories
.build/
config/.build/
chroot/
chroot.packages.live
chroot.packages.install
binary/
binary.modified/
cache/

# live-build generated config subdirs (regenerated by `lb config`)
config/binary
config/bootstrap
config/chroot
config/common
config/source

# build artifacts
*.iso
*.img
*.log
*.buildlog

# editor noise
*~
.DS_Store
